{
  "name": "scholar-agent-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
